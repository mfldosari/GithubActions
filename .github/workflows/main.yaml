name: Lint Test Deploy

on: push

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: code
        uses: actions/checkout@v4

      - name: install dep
        run: npm install

      - name: run linter
        run: npm run lint
